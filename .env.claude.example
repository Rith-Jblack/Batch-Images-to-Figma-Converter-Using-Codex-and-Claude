# =============================================================================
# Figma Converter Configuration
# Copy this file to .env and uncomment/modify values as needed.
# Environment variables always take precedence over values in this file.
# =============================================================================

# -- AI Provider --------------------------------------------------------------
# Which AI CLI to use: "claude" or "codex"
# AI_PROVIDER=claude

# -- Claude CLI Settings (used when AI_PROVIDER=claude) -----------------------
# CLAUDE_MODEL=claude-sonnet-4-5-20250929
# CLAUDE_MAX_TURNS=15
# CLAUDE_PARALLEL=3
# CLAUDE_DEBUG=0
# CLAUDE_TIMEOUT=600

# -- Codex CLI Settings (used when AI_PROVIDER=codex) -------------------------
# CODEX_MODEL=o4-mini
# CODEX_SANDBOX=workspace-write
# CODEX_PARALLEL=3
# CODEX_DEBUG=0
# CODEX_TIMEOUT=600

# -- Generic AI Settings (fallback if provider-specific vars not set) ---------
# AI_MODEL=
# AI_PARALLEL=3
# AI_DEBUG=0
# AI_TIMEOUT=600

# -- Paths (shared) -----------------------------------------------------------
# INPUT_DIR=../1-images-to-convert
# OUTPUT_DIR=../2-image-converted
# ARCHIVE_DIR=./3-image-archive
# PROMPT_TEMPLATE=../prompt-template.txt

# -- Model Reference -----------------------------------------------------------
# Claude models:
#   claude-sonnet-4-5-20250929   ~1min/image, ~$0.60/image  (recommended)
#   claude-opus-4-6              ~3min/image, ~$1.50/image   (highest quality)
#   claude-haiku-4-5-20251001    ~30s/image,  ~$0.10/image   (fastest/cheapest)
#
# Codex models:
#   o4-mini                      ~1min/image   (recommended)
#   o3                           ~2min/image   (higher quality)
